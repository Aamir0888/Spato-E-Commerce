<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Spato" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/spato_primary_color</item>
        <item name="colorPrimaryVariant">@color/spato_primary_color</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        // all checkbox color
        <item name="colorAccent">@color/spato_primary_color</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>

        // app icon showing before splash screen issue solution
        <item name="android:windowDisablePreview">true</item>
<!--        <item name="android:windowBackground">@null</item>-->

        //  disable dark mode
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>

    </style>

    //  pinView
    <style name="PinWidget.PinView">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/poppins_medium</item>
        <item name="itemBackground">@color/spato_secondary_color</item>
    </style>

    <style name="MyCustomTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/rounded_bottom_sheet</item>
    </style>

    <style name="CountryCodePicker">
        <item name="fontFamily">@font/poppins_regular</item>
        <item name="textFillColor">@color/white2</item>
    </style>

    <style name="loadingDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/loading_background_dialog</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <style name="CustomDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/custom_dialog_rounded_corner_background</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <!-- SearchView hint. -->
    <style name="AppSearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:textSize">@dimen/_9ssp</item>
        <item name="fontFamily">@font/poppins_regular</item>
        <item name="android:textColorHint">@android:color/white</item>
        <!-- Text color when the SearchView is expanded -->
        <item name="android:textColor">@android:color/white</item>
        <!-- Close button color (cross image) -->
        <item name="closeIconTint">@android:color/white</item>
        <item name="android:editTextColor">@android:color/white</item>
    </style>

</resources>